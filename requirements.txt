# Dependencies for the Streamlit dashboard.
# scikit-learn is HARD-pinned: pickled estimators are version-sensitive.
# numpy is held on 2.x: the model/scaler were pickled under numpy 2.x.
# Other pins are floors so the install can't fail on the Cloud Python version.
scikit-learn==1.6.1
numpy>=2.0,<3
pandas>=2.0,<3
joblib>=1.3
plotly>=5.20
streamlit>=1.40
